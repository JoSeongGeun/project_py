services:
  - type: web
    name: project-py
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
